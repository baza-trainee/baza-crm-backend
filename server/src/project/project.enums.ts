export enum ProjectStatuses {
  IN_SEARCH = 'searching',
  IN_PROGRESS = 'working',
  ENDED = 'ended',
}

export enum ProjectTypes {
  FREE = 'free',
  LIGHT = 'light',
  STRONG = 'strong',
}
